// Notes
 - Update table to display if pet received
 - Add button to event image
    - Embedded popup that shows event image
 - Create single player pages
    - Use URL parameter to determine which player
    - Table Layout
        - Each category is color coded to match screenshot
        - Header (Category, Skill Name, XP Gained, XP Start, XP End)
            - If max xp, change text style
            - If pet received, add pet icon
            - Category either hidden or shown once
        - Footer (Total XP, $total_xp, '', '')
    - Clickable sort bar similar to main page for each category filtering



 - git push heroku defy-total-xp-comp-2022:main